*:/enchant $enchantment [$level=1] = >>>
    _assertperm('admin', 1)
    @isValid = is_enchantment($enchantment);
    if (@isValid == true) {
        try {
            enchant_item(null, $enchantment, $level);
        } catch (EnchantmentException @e) {
            msg(color(RED).'That enchantment cannot be applied to that item.');
        } catch (RangeException @e) {
            msg(color(RED).'That enchantment cannot be applied at that level.');
        } catch (PlayerOfflineException @e) {
        }
    } else {
        msg(color(RED).'That is not a valid enchantment.');
    }
<<<
