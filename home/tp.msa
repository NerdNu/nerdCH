*:/tp $player = >>>
    _assertperm('restricted', 1)
    assign(@pl, player($player))
    if(not(is_null(@pl)),
        set_ploc(player(), ploc(@pl))
    )
<<<
 
*:/tphere $player = >>>
    _assertperm('restricted', 0)
    assign(@pl, player($player))
    if(not(is_null(@pl)),
        set_ploc(@pl, ploc(player()))
    )
<<<
