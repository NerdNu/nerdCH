#mods:/removenear $item $radius = if(gt($radius,25), die(color(red) 'Whoa, whoa, whoa. Step it down a notch, turbo.'), run(/removenear $item $radius))
//fixwater $radius = if(gt($radius, 25), die(color(red) 'Whoa, whoa, whoa. Step it down a notch, turbo.'), run(/fixwater $radius))
//fixlava $radius = if(gt($radius, 25), die(concat(color(red), 'TROGDOR THE BURNINATOR can only do up to //fixlava 25. Sorry.')), run(/fixlava $radius))
/fixwater $ = call_alias(//fixwater $)
/fixlava $ = call_alias(//fixlava $)

mods://removenear $ = >>> 
        assign(@match, parse_args($))
        if(equals(array_size(@match), 2),
                if(lt(@match[1], 25),
                        run(/removenear $),
                #else
                        msg(concat(color(red), '/removenear radius must be <=25'))),
        #else
                msg(concat(color(red), 'Syntax: /removenear block_id radius')))
<<<

mods:/removenear $ = call_alias(//removenear $)

/ri = /remove items 9999 \ /remove arrows 9999

/deselect = //sel cuboid
