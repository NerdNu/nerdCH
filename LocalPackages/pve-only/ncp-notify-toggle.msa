*:/ncp notify [$state] = >>>
	_assertperm("mod");
	if ($state=="on") {
		runas(~console, '/lp user '.player().' permission set nocheatplus.notify true');
		die('You will now recieve NCP notifications.');
	}
	if ($state=="off") {
		runas(~console, '/lp user '.player().' permission set nocheatplus.notify false');
		die('You will no longer recieve NCP notifications.');
	}
<<<
