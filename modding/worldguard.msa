#Create Region with proper flags and fix flags
*:/cregion $name $owner = /region define $name $owner \ /region flag $name use allow \ /region flag $name chest-access allow \ /region flag $name vehicle-place allow  \ /region flag $name vehicle-destroy allow
*:/cregion $name = msg('You must specify an owner as well. /cregion <name> <owner>')
*:/addflags $name = /region flag $name use allow \ /region flag $name chest-access allow \ /region flag $name vehicle-place allow  \ /region flag $name vehicle-destroy allow

*:/setspleef $block = >>>
    _assertperm('restricted', 0)
    runas(~op, /region select spleef_floor)
    runas(~op, //set $block)
    runas(~op, //sel cuboid)
<<<

#spleef:/spleef $id = for( assign( @firstx, 730 ), lte( @firstx, 743 ), inc( @firstx ), for( assign( @firsty, 185 ), lte( @firsty, 198 ), inc( @firsty ), set_block_at(@firstx,63,@firsty,$id) ) )

#TODO: Make this run /setspleef

*:/drunk_spleef $ = >>>
    _assertperm('restricted', 0)
	assign(@players, parse_args($))
	foreach(@players, @p,
		set_peffect(@p, 9, 1, 100)
	)
<<<
