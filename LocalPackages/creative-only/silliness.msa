/chickenhead = >>>
	foreach (@p in all_players()) {
		set_entity_rider(pinfo(@p, 13), spawn_entity('chicken')[0]);
	}
<<<

*:/uwot = msg(color('red').'==='.color('white').'M8'.color('red').'===');

*:/spoon = msg('Forks are better');
